name: Front End

on:
    push: 
        branches: 
            - 'dev'
    pull_request:
        branches:
            - 'dev'

jobs:
    hello:
        runs-on: ubuntu-lastest
        steps:
            - name: Hello steps
              run: echo "Hello word"

